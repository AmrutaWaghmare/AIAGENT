services:
  playwright:
    build: .
    working_dir: /app
    shm_size: 1gb
    command: ["npm", "test"]
    volumes:
      - ./test-results:/app/test-results
      - ./playwright-report:/app/playwright-report
